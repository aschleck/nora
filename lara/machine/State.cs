namespace nora.lara.machine {

    public enum SpectateState {

        REJECTED,

        DISCONNECTED,
        HANDSHAKE_REQUEST,
        HANDSHAKE_CONNECT,
        SIGNON_CONNECTED,
        SIGNON_NEW,
        SIGNON_PRESPAWN,
        SIGNON_SPAWN,
        SIGNON_FULL,
    }
}
